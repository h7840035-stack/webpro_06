<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= item.name %> - アイテム詳細</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <nav>
    <a href="/">トップ</a>
    <a href="/monsters/index">モンスター</a>
    <a href="/items/index">アイテム</a>
    <a href="/spells/index">呪文</a>
  </nav>

  <h1><%= item.name %></h1>
  
  <div class="detail-card">
    <dl>
      <dt>ID</dt>
      <dd><%= item.id %></dd>
      
      <dt>名前</dt>
      <dd><%= item.name %></dd>
      
      <dt>種類</dt>
      <dd><%= item.type %></dd>
      
      <dt>価格</dt>
      <dd><%= item.price %> G</dd>
      
      <dt>効果</dt>
      <dd><%= item.effect %></dd>
      
      <dt>装備可能職業</dt>
      <dd><%= item.equipable %></dd>
    </dl>
  </div>
  
  <div class="actions">
    <a href="/items/edit/<%= item.id %>" class="btn btn-primary">編集</a>
    <form action="/items/delete/<%= item.id %>" method="POST" style="display: inline;">
      <button type="submit" class="btn btn-danger" onclick="return confirm('本当に削除しますか？')">削除</button>
    </form>
    <a href="/items/index" class="btn btn-secondary">一覧に戻る</a>
  </div>
</body>
</html>
