<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= spell.name %>の編集 - DQ3管理システム</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <nav>
    <a href="/">トップ</a>
    <a href="/monsters/index">モンスター</a>
    <a href="/items/index">アイテム</a>
    <a href="/spells/index">呪文</a>
  </nav>

  <h1><%= spell.name %>の編集</h1>
  
  <form action="/spells/update/<%= spell.id %>" method="POST">
    <label for="name">名前</label>
    <input type="text" id="name" name="name" value="<%= spell.name %>" required>
    
    <label for="mp_cost">消費MP</label>
    <input type="number" id="mp_cost" name="mp_cost" value="<%= spell.mp_cost %>" required>
    
    <label for="type">種類</label>
    <select id="type" name="type" required>
      <option value="攻撃" <%= spell.type === '攻撃' ? 'selected' : '' %>>攻撃</option>
      <option value="回復" <%= spell.type === '回復' ? 'selected' : '' %>>回復</option>
      <option value="補助" <%= spell.type === '補助' ? 'selected' : '' %>>補助</option>
      <option value="移動" <%= spell.type === '移動' ? 'selected' : '' %>>移動</option>
    </select>
    
    <label for="effect">効果</label>
    <input type="text" id="effect" name="effect" value="<%= spell.effect %>" required>
    
    <label for="learner">習得職業</label>
    <input type="text" id="learner" name="learner" value="<%= spell.learner %>" required>
    
    <label for="level">習得レベル</label>
    <input type="number" id="level" name="level" value="<%= spell.level %>" required>
    
    <div class="actions">
      <button type="submit" class="btn btn-primary">更新</button>
      <a href="/spells/show/<%= spell.id %>" class="btn btn-secondary">キャンセル</a>
    </div>
  </form>
</body>
</html>
